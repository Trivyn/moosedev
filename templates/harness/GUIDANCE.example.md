<!--
An example. Copy it to `.moosedev/GUIDANCE.md`, edit it, and commit it.

Everything below this comment is the harness's compiled default. A GUIDANCE.md
REPLACES that default rather than adding to it, so what you leave here is what
the model gets. Copy it unchanged and nothing changes; edit a line and you have
reworded the default deliberately.

Text before the first `## Plan` or `## Implement` heading reaches both modes.
`## Plan` reaches planning; `## Implement` reaches approved work. Each mode
receives the shared text plus its own section, and each heading may appear once.
Any other heading is ordinary text, and so is a heading inside a fenced code
block. Comments like this one are stripped before the model sees the file.

One mode's share must fit 4096 bytes, and the whole file 12288.

Hard rules do not belong here. Record them in the project knowledge graph as
Constraints and Requirements: the harness delivers those as the Project rules
block and holds the plan to them.
-->
