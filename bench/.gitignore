.venv/
# Anchored: raw working dirs stay ignored, but the reviewed release/ copies are tracked.
/runs/
/corpus/
/scale/
__pycache__/
*.pyc

# Throwaway harness working dirs, temporal-bootstrap snapshots, and run logs (all regenerable).
_work/
_temporal_snapshots/
*.log
