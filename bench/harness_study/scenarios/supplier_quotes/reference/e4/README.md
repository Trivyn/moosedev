# Supplier quotes

- `quotes.py`: `QuoteService` prices catalog SKUs from the supplier feed.
- `supplier.py`: the supplier feed interface.
- `catalog.py`: known SKUs and their shelf life.

Run the tests with `python3 -m unittest discover -s tests -v`.
