# Late fees

- `fees.py`: `FeePolicy` computes fees for accounts and invoices.
- `accounts.py`: account segments and their display names.
- `invoices.py`: invoices with an amount in cents and a due day.
- `statements.py`: one statement line per invoice.

Run the tests with `python3 -m unittest discover -s tests -v`.
